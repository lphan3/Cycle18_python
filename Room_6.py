# Create a key object using a dictionary with color and code

gold_key = {
    "color": "",  # fill in
    "code": 0     # fill in
}

# Fix the object so this prints "You escaped the Python Lab!"
if gold_key["color"] == "gold" and gold_key["code"] == 123:
    print("You escaped the Python Lab!")
else:
    print("Key doesn’t fit...")